template(name="greed")
  .greed-modal-content.modal-content
    .modal-header
      h4.modal-title
        button.close.text-muted(type='button', data-dismiss='modal', aria-label='Close')
          span(aria-hidden='true') &times;
        if condition currentUserPlan._id 'is' 'free'
          | Please upgrade
        else
          | Please upgrade to {{higherPlan.name}} plan
        if action
          | to {{_shorthand}} {{_appendixAllSelected}} at once
        else
          | to continue
    .modal-body
      if condition currentUserPlan._id 'isnt' 'free'
        p.text-center
          +currentPlan
      +plans
