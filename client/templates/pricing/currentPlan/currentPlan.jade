template(name="currentPlan")
  span.current-plan
    | Currently you are on {{currentUserPlan.name}} plan, with {{currentUserExecutionsLeft}} runs left.
    // Commented out, because currentPlan is also used in greed modal
    //if currentUserCanUpgrade
    //  nobr If you like {{settings.public.name}}, please consider upgrading.
    //else
    //  nobr Thank you!
